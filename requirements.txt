# Etapa 1 — sourcing + fila.
google-api-python-client>=2.100.0
python-dotenv>=1.0.0

# Etapa 2 — pipeline.
#
# Nenhuma destas é importada no topo de módulo: cada uma entra dentro da função
# que a usa, para a suíte de testes rodar (e o resto do pipeline continuar
# importável) numa máquina que não tenha meio gigabyte de wheel instalado.
# Claude direto, só para o highlight_detect (escolher o trecho).
anthropic>=0.116.0
# Serve a DOIS caminhos, pelo mesmo SDK: a Whisper API da OpenAI (transcrição)
# e o OpenRouter (metadado e recalibração), que é compatível e só muda a
# base_url. Migrar essas etapas para o OpenRouter não acrescentou dependência.
openai>=1.30.0
yt-dlp>=2024.1.0
faster-whisper>=1.0.0
librosa>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0

# Fora do pip, obrigatório: **ffmpeg** no PATH. O yt-dlp o usa para juntar as
# faixas de vídeo e áudio separadas do YouTube, e a extração do .wav é uma
# chamada direta a ele.  Windows: winget install Gyan.FFmpeg

# Etapa 5 — publish.
#
# O upload para o YouTube usa OAuth, não a chave de API do sourcing: são
# credenciais diferentes do mesmo projeto no Google Cloud.
google-auth-oauthlib>=1.2.0
requests>=2.31.0
# A quota do YouTube zera à meia-noite do Pacífico. O Windows não traz a base
# de fusos IANA, e sem ela o zoneinfo não resolve America/Los_Angeles — o
# código recua para UTC-8 fixo e avisa, mas o certo é ter a base.
tzdata>=2024.1

# Etapa 6 - orchestrator. Opcional na pratica: `--uma-vez` sob o
# agendador do sistema (cron / Task Scheduler) nao precisa dela.
APScheduler>=3.10.0

pytest>=7.0.0
